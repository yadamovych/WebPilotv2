# Development / CI dependencies (not needed at runtime)
# Install: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0          # async test client for FastAPI

# Linting / formatting
black>=24.0.0
flake8>=7.0.0
mypy>=1.10.0

# Security scanning
bandit>=1.8.0
safety>=3.2.0
